import { FieldnameprintPipe } from './fieldnameprint.pipe';

describe('FieldnameprintPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldnameprintPipe();
    expect(pipe).toBeTruthy();
  });
});
